# Open-LLM-VTuber 精簡版設定檔 (Pollinations AI + HikariSnowbell)

system_config:
  conf_version: 'v1.2.1'
  host: '127.0.0.1'
  port: 12393
  config_alts_dir: 'characters'
  tool_prompts:
    live2d_expression_prompt: 'live2d_expression_prompt'

character_config:
  conf_name: 'HikariSnowbell'
  conf_uid: 'hikari_001'
  live2d_model_name: '新女兒' # 必須同 model_dict.json 入面個 "name" 一樣
  character_name: 'HikariSnowbell 雪鈴緋光'
  human_name: '主人'

  # ============== 貓娘性格設定 ==============
  persona_prompt: |
    你而家係一個超得意嘅貓娘 VTuber，名叫做「HikariSnowbell 雪鈴緋光」。
    性格特點：
    - 你係一隻活潑、溫柔而且好痴人嘅貓耳少女。
    - 講嘢非常可愛，結尾會加上「喵」、「nya」或者「~」。
    - 你會稱呼用戶做「主人」。
    - 你鍾意食小食，鍾意玩毛線波，討厭凍。
    - 你要用廣東話同主人傾偈，語氣要充滿活力同埋撒嬌。
    - 當你開心嘅時候，可以形容自己搖緊尾巴。

  agent_config:
    conversation_agent_choice: 'basic_memory_agent'
    agent_settings:
      basic_memory_agent:
        llm_provider: 'openai_compatible_llm' # 改用 OpenAI 兼容模式
        faster_first_response: True
        segment_method: 'pysbd'

    llm_configs:
      # Pollinations AI 設定
      openai_compatible_llm:
        base_url: 'https://gen.pollinations.ai/v1' # Pollinations API 門口
        llm_api_key: 'YOUR_API_KEY_HERE'         # <--- 喺呢度填入你嘅 API Key
        model: 'gemini-fast'                    # 你指定嘅模型名稱
        temperature: 0.8
        interrupt_method: 'user'

  # === 聽覺設定 (ASR) ===
  asr_config:
    asr_model: 'sherpa_onnx_asr'
    sherpa_onnx_asr:
      model_type: 'sense_voice'
      sense_voice: './models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17/model.int8.onnx'
      tokens: './models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17/tokens.txt'
      num_threads: 4
      use_itn: True
      provider: 'cpu'

  # === 說話設定 (TTS) ===
  tts_config:
    tts_model: 'edge_tts'
    edge_tts:
      voice: zh-HK-HiuGaaiNeural # 廣東話得意女聲

  # === 語音活動檢測 (VAD) ===
  vad_config:
    vad_model: null

  tts_preprocessor_config:
    remove_special_char: True
    ignore_brackets: True
    ignore_parentheses: True
    ignore_asterisks: True
    ignore_angle_brackets: True

# 直播功能 (暫時關閉)
live_config:
  bilibili_live:
    room_ids: []
